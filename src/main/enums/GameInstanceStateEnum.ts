export enum GameInstanceStateEnum {
    onStandby = "onStandby",
    validate = "validate",
    start = "start",
    startError = 'startError',
    close = "close",
    closeError = "closeError",
    stop = "stop"
}